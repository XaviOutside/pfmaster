# Application
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Database — MySQL 8
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="mysql://pfmaster:pfmaster@localhost:3306/pfmaster"

# Docker MySQL credentials (used by docker-compose.yml)
DB_ROOT_PASSWORD=rootpassword
DB_NAME=pfmaster
DB_USER=pfmaster
DB_PASSWORD=pfmaster
