/**
 * Pagination constants shared across all frontend hooks and services.
 *
 * DEFAULT_PAGE_SIZE is the default limit for list API calls.
 * All hooks and API service functions import from here.
 */
export const DEFAULT_PAGE_SIZE = 20;
